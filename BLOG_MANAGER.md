# 博客管理工具

双击项目根目录中的 `博客管理器.exe` 即可打开中文图形界面，不会显示命令行窗口。

## 推荐用法

本地修改完成后点击 **安全发布**。工具会依次：

1. 获取 GitHub 和 Pages CMS 的最新提交；
2. 检查文章日期格式；
3. 在安装了 Hugo 时执行本地构建；
4. 提交本地修改；
5. 将本地提交变基到最新远端版本；
6. 正常推送到 `main`，触发 Cloudflare 部署。

工具不会使用强制推送。网络失败、日期无效、构建失败或内容冲突时，都会在推送前停止。

## 菜单功能

- **安全发布**：安全同步、检查、提交和推送；日常发布使用此项。
- **同步内容**：本地没有修改时，从 Pages CMS 和 GitHub 同步最新内容。
- **检查状态**：查看当前分支、未提交文件和最近五次提交。
- **构建检查**：检查日期并运行 Hugo 正式构建。
- **本地预览**：运行包含草稿的 Hugo 本地预览服务器。
- **新建文章**：按标准日期格式创建新的 Markdown 草稿。
- **打开网站**：打开 `hugo.toml` 中配置的网站。
- **打开 Pages CMS**：打开 Pages CMS。

“构建检查”与“本地预览”需要本机安装 Hugo。未安装 Hugo 时，“安全发布”仍会完成日期检查并提示跳过本地构建。

也可以在 PowerShell 中直接调用命令：

```powershell
.\blog-manager.ps1 status
.\blog-manager.ps1 sync
.\blog-manager.ps1 publish
.\blog-manager.ps1 build
.\blog-manager.ps1 serve
```

如果“安全发布”提示冲突，它不会推送或覆盖远端内容。此时应检查双方对同一文章的修改后再重新发布。
